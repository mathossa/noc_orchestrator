import { LoadingState } from '@/components/ui/page-state'

export default function ApplicationLoading() {
  return <LoadingState title="Loading workspace" description="Preparing firmware lifecycle data…" />
}
