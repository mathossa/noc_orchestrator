import { SectionPlaceholder } from '@/components/ui/section-placeholder'

export default function ModelsPage() {
  return (
    <SectionPlaceholder
      title="Models"
      description="Device models connect vendor and device type to desired firmware policy."
      emptyTitle="Model management is not connected yet"
      emptyDescription="The route and shared UI foundation are ready for the model catalog implementation."
    />
  )
}
